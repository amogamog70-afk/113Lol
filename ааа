-- ================================================================= --
--                     VOLTCLIPS UI LIBRARY (NEVERLOSE STYLE)        --
-- ================================================================= --

local TweenService = game:GetService("TweenService")
local UserInputService = game:GetService("UserInputService")
local CoreGui = game:GetService("CoreGui")

local VoltClips = {}
VoltClips.__index = VoltClips

-- Палитра Neverlose
local Theme = {
    Background = Color3.fromRGB(15, 17, 21),
    Sidebar = Color3.fromRGB(20, 22, 28),
    Card = Color3.fromRGB(24, 26, 33),
    CardOutline = Color3.fromRGB(35, 38, 48),
    Accent = Color3.fromRGB(100, 130, 255),
    TextPrimary = Color3.fromRGB(240, 240, 245),
    TextDark = Color3.fromRGB(120, 125, 140),
    ElementBg = Color3.fromRGB(18, 19, 24)
}

-- Вспомогательная функция закругления
local function AddCorner(parent, radius)
    local corner = Instance.new("UICorner")
    corner.CornerRadius = UDim.new(0, radius or 8)
    corner.Parent = parent
    return corner
end

-- Вспомогательная функция обводки
local function AddStroke(parent, color, thickness)
    local stroke = Instance.new("UIStroke")
    stroke.Color = color or Theme.CardOutline
    stroke.Thickness = thickness or 1
    stroke.ApplyStrokeMode = Enum.ApplyStrokeMode.Border
    stroke.Parent = parent
    return stroke
end

function VoltClips:CreateWindow(titleText)
    local window = {}
    
    -- Gui Root
    local gui = Instance.new("ScreenGui")
    gui.Name = "VoltClips_UI"
    gui.ResetOnSpawn = false
    
    -- Защита от обнаружения (Пытаемся пихать в CoreGui)
    pcall(function()
        gui.Parent = CoreGui
    end)
    if not gui.Parent then
        gui.Parent = game:GetService("Players").LocalPlayer:WaitForChild("PlayerGui")
    end

    -- Главный Фрейм
    local mainFrame = Instance.new("Frame")
    mainFrame.Name = "MainFrame"
    mainFrame.Size = UDim2.new(0, 750, 0, 500)
    mainFrame.Position = UDim2.new(0.5, -375, 0.5, -250)
    mainFrame.BackgroundColor3 = Theme.Background
    mainFrame.BorderSizePixel = 0
    mainFrame.ClipsDescendants = false
    mainFrame.Parent = gui

    AddCorner(mainFrame, 12)
    AddStroke(mainFrame, Theme.CardOutline, 1)

    -- Перетаскивание (Draggable)
    local dragging, dragInput, dragStart, startPos
    mainFrame.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 then
            dragging = true
            dragStart = input.Position
            startPos = mainFrame.Position
        end
    end)
    mainFrame.InputEnded:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 then
            dragging = false
        end
    end)
    UserInputService.InputChanged:Connect(function(input)
        if dragging and input.UserInputType == Enum.UserInputType.MouseMovement then
            local delta = input.Position - dragStart
            mainFrame.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
        end
    end)

    -- Левый Сайдбар
    local sidebar = Instance.new("Frame")
    sidebar.Name = "Sidebar"
    sidebar.Size = UDim2.new(0, 60, 1, 0)
    sidebar.BackgroundColor3 = Theme.Sidebar
    sidebar.BorderSizePixel = 0
    sidebar.Parent = mainFrame
    AddCorner(sidebar, 12)

    -- Логотип
    local logo = Instance.new("TextLabel")
    logo.Size = UDim2.new(1, 0, 0, 50)
    logo.BackgroundTransparency = 1
    logo.Text = "VC"
    logo.TextColor3 = Theme.Accent
    logo.TextSize = 22
    logo.Font = Enum.Font.GothamBold
    logo.Parent = sidebar

    -- Контейнер для главных иконок слева
    local tabContainer = Instance.new("Frame")
    tabContainer.Size = UDim2.new(1, 0, 1, -60)
    tabContainer.Position = UDim2.new(0, 0, 0, 50)
    tabContainer.BackgroundTransparency = 1
    tabContainer.Parent = sidebar

    local tabLayout = Instance.new("UIListLayout")
    tabLayout.HorizontalAlignment = Enum.HorizontalAlignment.Center
    tabLayout.Padding = UDim.new(0, 10)
    tabLayout.Parent = tabContainer

    -- Верхний Бар (TopBar)
    local topBar = Instance.new("Frame")
    topBar.Name = "TopBar"
    topBar.Size = UDim2.new(1, -70, 0, 45)
    topBar.Position = UDim2.new(0, 70, 0, 5)
    topBar.BackgroundTransparency = 1
    topBar.Parent = mainFrame

    -- Поиск (Placeholder)
    local searchBar = Instance.new("Frame")
    searchBar.Size = UDim2.new(0, 180, 0, 30)
    searchBar.Position = UDim2.new(1, -190, 0.5, -15)
    searchBar.BackgroundColor3 = Theme.Card
    searchBar.Parent = topBar
    AddCorner(searchBar, 8)
    AddStroke(searchBar, Theme.CardOutline, 1)

    local searchIcon = Instance.new("TextLabel")
    searchIcon.Size = UDim2.new(0, 30, 1, 0)
    searchIcon.BackgroundTransparency = 1
    searchIcon.Text = "🔍"
    searchIcon.TextSize = 12
    searchIcon.TextColor3 = Theme.TextDark
    searchIcon.Parent = searchBar

    local searchInput = Instance.new("TextBox")
    searchInput.Size = UDim2.new(1, -35, 1, 0)
    searchInput.Position = UDim2.new(0, 30, 0, 0)
    searchInput.BackgroundTransparency = 1
    searchInput.Text = ""
    searchInput.PlaceholderText = "Search..."
    searchInput.PlaceholderColor3 = Theme.TextDark
    searchInput.TextColor3 = Theme.TextPrimary
    searchInput.Font = Enum.Font.Gotham
    searchInput.TextSize = 13
    searchInput.TextXAlignment = Enum.TextXAlignment.Left
    searchInput.Parent = searchBar

    -- Контейнер Подвкладок (SubTabs) в TopBar
    local subTabContainer = Instance.new("Frame")
    subTabContainer.Size = UDim2.new(1, -200, 1, 0)
    subTabContainer.BackgroundTransparency = 1
    subTabContainer.Parent = topBar

    local subTabLayout = Instance.new("UIListLayout")
    subTabLayout.FillDirection = Enum.FillDirection.Horizontal
    subTabLayout.Padding = UDim.new(0, 8)
    subTabLayout.VerticalAlignment = Enum.VerticalAlignment.Center
    subTabLayout.Parent = subTabContainer

    -- Основная Зона Контента
    local contentArea = Instance.new("Frame")
    contentArea.Name = "ContentArea"
    contentArea.Size = UDim2.new(1, -75, 1, -60)
    contentArea.Position = UDim2.new(0, 70, 0, 55)
    contentArea.BackgroundTransparency = 1
    contentArea.Parent = mainFrame

    -- Логика переключения Главных Вкладок
    local tabs = {}
    local activeTab = nil

    function window:AddTab(iconId)
        local tab = { SubTabs = {} }

        local tabButton = Instance.new("TextButton")
        tabButton.Size = UDim2.new(0, 40, 0, 40)
        tabButton.BackgroundColor3 = Theme.ElementBg
        tabButton.Text = iconId or "★"
        tabButton.TextColor3 = Theme.TextDark
        tabButton.TextSize = 18
        tabButton.Font = Enum.Font.GothamBold
        tabButton.AutoButtonColor = false
        tabButton.Parent = tabContainer
        AddCorner(tabButton, 8)

        tab.Button = tabButton

        function tab:AddSubTab(name)
            local subTab = {}

            -- Кнопка подвкладки (Main, Camera, HUD...)
            local subButton = Instance.new("TextButton")
            subButton.Size = UDim2.new(0, 80, 0, 28)
            subButton.BackgroundColor3 = Theme.Card
            subButton.Text = name
            subButton.TextColor3 = Theme.TextDark
            subButton.Font = Enum.Font.GothamMedium
            subButton.TextSize = 13
            subButton.Parent = subTabContainer
            subButton.Visible = false
            AddCorner(subButton, 6)

            -- Двухколоночный фрейм подвкладки
            local subPage = Instance.new("Frame")
            subPage.Size = UDim2.new(1, -10, 1, 0)
            subPage.BackgroundTransparency = 1
            subPage.Visible = false
            subPage.Parent = contentArea

            local leftColumn = Instance.new("ScrollingFrame")
            leftColumn.Size = UDim2.new(0.49, 0, 1, 0)
            leftColumn.BackgroundTransparency = 1
            leftColumn.ScrollBarThickness = 0
            leftColumn.Parent = subPage

            local rightColumn = Instance.new("ScrollingFrame")
            rightColumn.Size = UDim2.new(0.49, 0, 1, 0)
            rightColumn.Position = UDim2.new(0.51, 0, 0, 0)
            rightColumn.BackgroundTransparency = 1
            rightColumn.ScrollBarThickness = 0
            rightColumn.Parent = subPage

            local leftLayout = Instance.new("UIListLayout")
            leftLayout.Padding = UDim.new(0, 10)
            leftLayout.Parent = leftColumn

            local rightLayout = Instance.new("UIListLayout")
            rightLayout.Padding = UDim.new(0, 10)
            rightLayout.Parent = rightColumn

            subTab.Button = subButton
            subTab.Page = subPage

            -- Секция (Карточка в Neverlose стилистике)
            function subTab:AddSection(sectionTitle, side)
                local section = {}
                local parentCol = (side == "right") and rightColumn or leftColumn

                local card = Instance.new("Frame")
                card.Name = "PuzzlePiece" -- Для Анимации
                card.Size = UDim2.new(1, -5, 0, 40)
                card.BackgroundColor3 = Theme.Card
                card.Parent = parentCol
                AddCorner(card, 8)
                AddStroke(card, Theme.CardOutline, 1)

                local cardTitle = Instance.new("TextLabel")
                cardTitle.Size = UDim2.new(1, -20, 0, 25)
                cardTitle.Position = UDim2.new(0, 10, 0, 5)
                cardTitle.BackgroundTransparency = 1
                cardTitle.Text = sectionTitle
                cardTitle.TextColor3 = Theme.TextPrimary
                cardTitle.Font = Enum.Font.GothamBold
                cardTitle.TextSize = 13
                cardTitle.TextXAlignment = Enum.TextXAlignment.Left
                cardTitle.Parent = card

                local container = Instance.new("Frame")
                container.Size = UDim2.new(1, -20, 1, -30)
                container.Position = UDim2.new(0, 10, 0, 30)
                container.BackgroundTransparency = 1
                container.Parent = card

                local cLayout = Instance.new("UIListLayout")
                cLayout.Padding = UDim.new(0, 8)
                cLayout.Parent = container

                local function resizeCard()
                    card.Size = UDim2.new(1, -5, 0, cLayout.AbsoluteContentSize.Y + 40)
                end
                cLayout:GetPropertyChangedSignal("AbsoluteContentSize"):Connect(resizeCard)

                -- 1. КНОПКА (Button)
                function section:AddButton(text, callback)
                    local btn = Instance.new("TextButton")
                    btn.Size = UDim2.new(1, 0, 0, 28)
                    btn.BackgroundColor3 = Theme.ElementBg
                    btn.Text = text
                    btn.TextColor3 = Theme.TextPrimary
                    btn.Font = Enum.Font.Gotham
                    btn.TextSize = 12
                    btn.Parent = container
                    AddCorner(btn, 6)
                    AddStroke(btn, Theme.CardOutline, 1)

                    btn.MouseButton1Click:Connect(function()
                        TweenService:Create(btn, TweenInfo.new(0.1), {BackgroundColor3 = Theme.Accent}):Play()
                        task.wait(0.1)
                        TweenService:Create(btn, TweenInfo.new(0.1), {BackgroundColor3 = Theme.ElementBg}):Play()
                        if callback then callback() end
                    end)
                end

                -- 2. ГАЛОЧКА (Toggle)
                function section:AddToggle(text, default, callback)
                    local state = default or false
                    
                    local toggleFrame = Instance.new("Frame")
                    toggleFrame.Size = UDim2.new(1, 0, 0, 24)
                    toggleFrame.BackgroundTransparency = 1
                    toggleFrame.Parent = container

                    local box = Instance.new("TextButton")
                    box.Size = UDim2.new(0, 18, 0, 18)
                    box.Position = UDim2.new(0, 0, 0.5, -9)
                    box.BackgroundColor3 = state and Theme.Accent or Theme.ElementBg
                    box.Text = ""
                    box.Parent = toggleFrame
                    AddCorner(box, 4)
                    AddStroke(box, Theme.CardOutline, 1)

                    local label = Instance.new("TextLabel")
                    label.Size = UDim2.new(1, -30, 1, 0)
                    label.Position = UDim2.new(0, 26, 0, 0)
                    label.BackgroundTransparency = 1
                    label.Text = text
                    label.TextColor3 = state and Theme.TextPrimary or Theme.TextDark
                    label.Font = Enum.Font.Gotham
                    label.TextSize = 12
                    label.TextXAlignment = Enum.TextXAlignment.Left
                    label.Parent = toggleFrame

                    local function update()
                        TweenService:Create(box, TweenInfo.new(0.15), {BackgroundColor3 = state and Theme.Accent or Theme.ElementBg}):Play()
                        TweenService:Create(label, TweenInfo.new(0.15), {TextColor3 = state and Theme.TextPrimary or Theme.TextDark}):Play()
                        if callback then callback(state) end
                    end

                    box.MouseButton1Click:Connect(function()
                        state = not state
                        update()
                    end)
                end

                -- 3. СЛАЙДЕР (Slider)
                function section:AddSlider(text, min, max, default, callback)
                    local val = default or min
                    
                    local sliderFrame = Instance.new("Frame")
                    sliderFrame.Size = UDim2.new(1, 0, 0, 36)
                    sliderFrame.BackgroundTransparency = 1
                    sliderFrame.Parent = container

                    local title = Instance.new("TextLabel")
                    title.Size = UDim2.new(1, 0, 0, 15)
                    title.BackgroundTransparency = 1
                    title.Text = text
                    title.TextColor3 = Theme.TextDark
                    title.Font = Enum.Font.Gotham
                    title.TextSize = 11
                    title.TextXAlignment = Enum.TextXAlignment.Left
                    title.Parent = sliderFrame

                    local valLabel = Instance.new("TextLabel")
                    valLabel.Size = UDim2.new(1, 0, 0, 15)
                    valLabel.BackgroundTransparency = 1
                    valLabel.Text = tostring(val)
                    valLabel.TextColor3 = Theme.TextPrimary
                    valLabel.Font = Enum.Font.Gotham
                    valLabel.TextSize = 11
                    valLabel.TextXAlignment = Enum.TextXAlignment.Right
                    valLabel.Parent = sliderFrame

                    local bar = Instance.new("Frame")
                    bar.Size = UDim2.new(1, 0, 0, 6)
                    bar.Position = UDim2.new(0, 0, 0, 22)
                    bar.BackgroundColor3 = Theme.ElementBg
                    bar.Parent = sliderFrame
                    AddCorner(bar, 3)

                    local fill = Instance.new("Frame")
                    fill.Size = UDim2.new((val - min)/(max - min), 0, 1, 0)
                    fill.BackgroundColor3 = Theme.Accent
                    fill.Parent = bar
                    AddCorner(fill, 3)

                    local sliding = false
                    local function setVal(input)
                        local pos = math.clamp((input.Position.X - bar.AbsolutePosition.X) / bar.AbsoluteSize.X, 0, 1)
                        val = math.floor(min + ((max - min) * pos))
                        valLabel.Text = tostring(val)
                        fill.Size = UDim2.new(pos, 0, 1, 0)
                        if callback then callback(val) end
                    end

                    bar.InputBegan:Connect(function(input)
                        if input.UserInputType == Enum.UserInputType.MouseButton1 then
                            sliding = true
                            setVal(input)
                        end
                    end)
                    UserInputService.InputEnded:Connect(function(input)
                        if input.UserInputType == Enum.UserInputType.MouseButton1 then sliding = false end
                    end)
                    UserInputService.InputChanged:Connect(function(input)
                        if sliding and input.UserInputType == Enum.UserInputType.MouseMovement then setVal(input) end
                    end)
                end

                -- 4. ТЕКСТБОКС (TextBox)
                function section:AddTextBox(text, placeholder, callback)
                    local boxFrame = Instance.new("Frame")
                    boxFrame.Size = UDim2.new(1, 0, 0, 28)
                    boxFrame.BackgroundColor3 = Theme.ElementBg
                    boxFrame.Parent = container
                    AddCorner(boxFrame, 6)
                    AddStroke(boxFrame, Theme.CardOutline, 1)

                    local input = Instance.new("TextBox")
                    input.Size = UDim2.new(1, -10, 1, 0)
                    input.Position = UDim2.new(0, 5, 0, 0)
                    input.BackgroundTransparency = 1
                    input.Text = ""
                    input.PlaceholderText = placeholder or text
                    input.PlaceholderColor3 = Theme.TextDark
                    input.TextColor3 = Theme.TextPrimary
                    input.Font = Enum.Font.Gotham
                    input.TextSize = 12
                    input.TextXAlignment = Enum.TextXAlignment.Left
                    input.Parent = boxFrame

                    input.FocusLost:Connect(function(enterPressed)
                        if callback then callback(input.Text) end
                    end)
                end

                return section
            end

            -- Логика переключения подвкладки
            subButton.MouseButton1Click:Connect(function()
                for _, st in pairs(tab.SubTabs) do
                    st.Button.TextColor3 = Theme.TextDark
                    st.Button.BackgroundColor3 = Theme.Card
                    st.Page.Visible = false
                end
                subButton.TextColor3 = Theme.Accent
                subButton.BackgroundColor3 = Theme.Sidebar
                subPage.Visible = true
            end)

            table.insert(tab.SubTabs, subTab)

            -- Активируем первую подвкладку по умолчанию
            if #tab.SubTabs == 1 then
                subButton.TextColor3 = Theme.Accent
                subButton.BackgroundColor3 = Theme.Sidebar
                subPage.Visible = true
            end

            return subTab
        end

        -- Активация Главной Вкладки
        tabButton.MouseButton1Click:Connect(function()
            for _, t in pairs(tabs) do
                t.Button.TextColor3 = Theme.TextDark
                t.Button.BackgroundColor3 = Theme.ElementBg
                for _, st in pairs(t.SubTabs) do
                    st.Button.Visible = false
                    st.Page.Visible = false
                end
            end
            tabButton.TextColor3 = Theme.Accent
            tabButton.BackgroundColor3 = Theme.Sidebar
            
            for _, st in pairs(tab.SubTabs) do
                st.Button.Visible = true
            end
            if #tab.SubTabs > 0 then
                tab.SubTabs[1].Page.Visible = true
                tab.SubTabs[1].Button.TextColor3 = Theme.Accent
            end
        end)

        table.insert(tabs, tab)

        if #tabs == 1 then
            tabButton.TextColor3 = Theme.Accent
            tabButton.BackgroundColor3 = Theme.Sidebar
        end

        return tab
    end

    -- ================================================================= --
    --      АНИМАЦИЯ РАЗРУШЕНИЯ И СБОРКИ НА PAZLE (RIGHT SHIFT TOGGLE)   --
    -- ================================================================= --
    local isMenuOpen = true
    local isAnimating = false

    local function ShatterAndAssemble()
        if isAnimating then return end
        isAnimating = true

        -- Собираем элементы-куски
        local pieces = {}
        for _, desc in pairs(mainFrame:GetDescendants()) do
            if desc:IsA("Frame") and desc.Name == "PuzzlePiece" then
                table.insert(pieces, desc)
            end
        end

        if isMenuOpen then
            -- [ АНИМАЦИЯ ЛОМКИ / РАЗЛЕТА ]
            for _, piece in pairs(pieces) do
                local randomX = math.random(-300, 300)
                local randomY = math.random(-300, 300)
                local randomRot = math.random(-90, 90)

                TweenService:Create(piece, TweenInfo.new(0.4, Enum.EasingStyle.Back, Enum.EasingDirection.In), {
                    Position = piece.Position + UDim2.new(0, randomX, 0, randomY),
                    Rotation = randomRot,
                    BackgroundTransparency = 1
                }):Play()
            end

            -- Анимация растворения самого корпуса
            TweenService:Create(mainFrame, TweenInfo.new(0.5, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
                Size = UDim2.new(0, 750, 0, 0),
                BackgroundTransparency = 1
            }):Play()

            task.wait(0.4)
            mainFrame.Visible = false
            isMenuOpen = false
        else
            -- [ АНИМАЦИЯ СБОРКИ ПАЗЛА ИЗ КУСКОВ ]
            mainFrame.Visible = true
            TweenService:Create(mainFrame, TweenInfo.new(0.4, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {
                Size = UDim2.new(0, 750, 0, 500),
                BackgroundTransparency = 0
            }):Play()

            for _, piece in pairs(pieces) do
                -- Сбрасываем в исходники
                TweenService:Create(piece, TweenInfo.new(0.5, Enum.EasingStyle.Quart, Enum.EasingDirection.Out), {
                    Position = UDim2.new(0, 0, piece.Position.Y.Scale, piece.Position.Y.Offset),
                    Rotation = 0,
                    BackgroundTransparency = 0
                }):Play()
            end

            task.wait(0.5)
            isMenuOpen = true
        end

        isAnimating = false
    end

    -- Биндим клавишу Right Shift
    UserInputService.InputBegan:Connect(function(input, gpe)
        if not gpe and input.KeyCode == Enum.KeyCode.RightShift then
            ShatterAndAssemble()
        end
    end)

    return window
end

-- ================================================================= --
--                     ПРИМЕР ИСПОЛЬЗОВАНИЯ (TEST CODE)             --
-- ================================================================= --

local Window = VoltClips:CreateWindow("VoltClips")

-- Создаем Главные Вкладки (Сайдбар слева)
local Tab1 = Window:AddTab("🎯") -- Visuals / Aim
local Tab2 = Window:AddTab("⚡") -- Movement

-- Создаем Подвкладки (Сверху)
local MainSubTab = Tab1:AddSubTab("main")
local CameraSubTab = Tab1:AddSubTab("camera")
local HudSubTab = Tab1:AddSubTab("hud")

-- Добавляем Секцию в Left Column
local HitLogsSection = MainSubTab:AddSection("hit logs", "left")
HitLogsSection:AddToggle("Hit logs", true, function(val)
    print("Hit logs:", val)
end)
HitLogsSection:AddToggle("Console logs", false, function(val)
    print("Console logs:", val)
end)
HitLogsSection:AddSlider("Hit Sound Volume", 0, 100, 50, function(val)
    print("Volume:", val)
end)
HitLogsSection:AddTextBox("Custom Sound Name", "Type sound here...", function(txt)
    print("Sound set to:", txt)
end)

-- Добавляем Секцию в Right Column
local MovementSection = MainSubTab:AddSection("movement helper", "right")
MovementSection:AddToggle("Bunnyhop", true, function(val)
    print("BHop:", val)
end)
MovementSection:AddToggle("Auto Strafer", false, function(val)
    print("Strafer:", val)
end)
MovementSection:AddButton("Reset Settings", function()
    print("Settings reseted!")
end)
